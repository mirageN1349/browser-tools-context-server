{
  /// Port for the browser-tools-server connection (Default: 3025)
  // "port": 3025
  /// Host for the browser-tools-server connection (Default: 127.0.0.1)
  // "host": "127.0.0.1"
  /// Command to start the MCP server (Default: node)
  // "server_command": "node"
  /// Arguments for the server command (Default: uses built-in wrapper script)
  // "server_args": ["-e", "..."]
}
